#include "ListNode_template.h"
template <typename T> class List
{
 private: int _size;//规模
          Posi(T) header;//头哨兵
		  Posi(T) trailer;//尾哨兵
 protected: //内部函数
 public: //构造函数，析构函数，只读接口，可写接口，遍历接口...
};